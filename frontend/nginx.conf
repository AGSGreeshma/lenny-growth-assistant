server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Single-page app: any path that isn't a real file falls back to
    # index.html so client-side routing (if added later) still works.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
